# Emberapp

## Starting up

Start rails server with

```
rails server
```

and Ember-CLI server with

```
cd emberapp && ember server --proxy http://localhost:3000/
```

## HTTP Auth

Credentials are:

**Login:** `admin`

**Password:** `password`
