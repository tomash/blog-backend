import { inflectByCount } from '../../../helpers/inflect-by-count';
import { module, test } from 'qunit';

module('Unit | Helper | inflect by count');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = inflectByCount(42);
  assert.ok(result);
});
